export interface Admin {
    id: string;
    lastName: string;
    firstName: string;
    email: string;
    role: string;
    code: string;
}

export interface AdminResponse {
    id: string;
    lastName: string;
    firstName: string;
    email: string;
    role: string;
    code: string;
}